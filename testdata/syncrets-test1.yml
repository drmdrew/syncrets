vault:
    vault-a:
        url: http://localhost:8201
        auth: token
        token:
            file: .vault-a-token
    vault-b:
        url: http://localhost:8202
        auth: token
        token:
            file: .vault-b-token
    vault-b-user:
        url: http://localhost:8202
        auth: user
        token:
            file: .vault-b-token
